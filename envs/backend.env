# Database
DB_HOST=db                # Name of the database service in docker-compose
DB_PORT=5432              # Postgres default port
DB_USER=user              # DB username
DB_PASSWORD=password      # DB password
DB_NAME=mydb              # DB name

# App secret
SECRET_KEY=supersecret    # Used for JWT or encryption

# Optional
DEBUG=True                # For development logging
